---

- name: Read Hosts
  hosts: dhcp
  
  tasks:
  - read_csv:
        path: config.csv
        key: HOSTNAME
      register: vms
    - debug:
        msg: "{{ item.key }}:
              {{ item.value.HOSTNAME }},
              {{ item.value.IP }},
              {{ item.value.MAC }}"
      loop: "{{ vms.dict|dict2items }}"

